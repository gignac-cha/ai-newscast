{
  "name": "@ai-newscast/newscast-scheduler-worker",
  "version": "1.0.0",
  "description": "Cloudflare Workers scheduler for AI newscast pipeline orchestration",
  "type": "module",
  "scripts": {
    "build": "node build.ts",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "deploy": "pnpm build && wrangler deploy"
  },
  "dependencies": {
    "@ai-newscast/core": "workspace:*",
    "@ai-newscast/core-worker": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251011.0",
    "esbuild": "^0.25.11",
    "typescript": "^5.9.3",
    "wrangler": "^4.43.0"
  }
}
